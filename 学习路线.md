# 🎓 AI 工作流系统 - 新手学习路线

## 📋 项目概览

这是一个基于 **Next.js + Prisma + ReactFlow** 构建的 AI 工作流自动化系统，类似于 Coze.cn 或 FlowGram.AI 的工作流功能。

**核心技术栈：**
- **前端框架**：Next.js 15 (React 19) + TypeScript
- **UI 组件**：ReactFlow（可视化编辑器）+ Tailwind CSS
- **数据库 ORM**：Prisma + SQLite
- **AI 集成**：OpenAI SDK（兼容国内AI厂商）
- **执行引擎**：自研 DAG（有向无环图）执行引擎

---

## 🗺️ 学习路线图

```
第1阶段：理解项目架构（1-2天）
    ↓
第2阶段：掌握数据流转（2-3天）
    ↓
第3阶段：学习前端界面（2-3天）
    ↓
第4阶段：深入执行引擎（3-4天）
    ↓
第5阶段：实践与扩展（持续）
```

---

## 📚 第1阶段：理解项目架构（1-2天）

### 目标
- 了解项目的整体结构
- 理解数据库模型和关系
- 掌握基本的配置文件

### 学习路径

#### 1.1 从配置文件开始（30分钟）

**阅读顺序：**

1. **`package.json`** - 项目依赖和脚本
   - 关键依赖：`next`, `@prisma/client`, `@xyflow/react`, `openai`
   - 启动命令：`npm run dev`
   
2. **`tsconfig.json`** - TypeScript 配置
   - 路径别名：`@/*` 映射到 `./src/*`
   - 编译选项：严格模式、模块解析
   
3. **`next.config.ts`** - Next.js 配置
   - 默认配置，暂无自定义

**实操练习：**
```bash
# 克隆或打开项目
cd AI-workflow

# 安装依赖
npm install

# 查看依赖树
npm list --depth=0

# 启动开发服务器
npm run dev
```

#### 1.2 理解数据库模型（1-2小时）

**阅读文件：`prisma/schema.prisma`**

这是整个项目的数据基础，必须深入理解！

**核心模型关系图：**
```
Workflow（工作流）
    ├── WorkflowNode[]（节点）
    │       ├── incoming: WorkflowEdge[]（输入连接）
    │       ├── outgoing: WorkflowEdge[]（输出连接）
    │       └── steps: Step[]（执行步骤）
    ├── WorkflowEdge[]（连接线）
    └── Run[]（运行记录）
            └── Step[]（执行步骤）
```

**学习重点：**
1. **Workflow**：存储工作流基本信息
2. **WorkflowNode**：存储节点（含类型、配置、位置）
3. **WorkflowEdge**：存储节点间的连接关系
4. **Run**：记录每次执行（含状态、输入、上下文）
5. **Step**：记录每个节点的执行详情

**实操练习：**
```bash
# 运行数据库迁移
npx prisma migrate dev

# 打开 Prisma Studio（可视化数据库管理）
npx prisma studio
```

在 Prisma Studio 中：
- 查看每个表的结构
- 创建测试数据
- 理解表之间的关系

#### 1.3 梳理项目目录结构（30分钟）

```
AI-workflow/
├── prisma/              # 数据库相关
│   ├── schema.prisma    # 数据库模型定义 ⭐
│   ├── dev.db           # SQLite 数据库文件
│   └── migrations/      # 数据库迁移记录
│
├── src/
│   ├── app/             # Next.js App Router
│   │   ├── api/         # API 路由 ⭐
│   │   │   ├── users/
│   │   │   ├── workflows/
│   │   │   └── runs/
│   │   ├── workflows/   # 工作流页面 ⭐
│   │   ├── runs/        # 运行结果页面
│   │   ├── layout.tsx   # 根布局
│   │   ├── page.tsx     # 首页
│   │   └── globals.css  # 全局样式
│   │
│   └── lib/             # 核心库 ⭐⭐⭐
│       ├── engine.ts    # 工作流执行引擎（最重要）
│       ├── prisma.ts    # 数据库客户端
│       ├── openai.ts    # AI 客户端
│       └── utils/
│           └── interpolate.ts  # 模板插值工具
│
├── package.json
├── tsconfig.json
├── next.config.ts
└── README.md
```

**标记说明：**
- ⭐ 重要文件
- ⭐⭐ 非常重要
- ⭐⭐⭐ 核心文件（必须深入理解）

---

## 🔄 第2阶段：掌握数据流转（2-3天）

### 目标
- 理解数据如何在系统中流动
- 掌握执行引擎的工作原理
- 学习模板插值机制

### 学习路径

#### 2.1 核心工具函数（1小时）

**阅读文件：`src/lib/utils/interpolate.ts`**

这个文件虽小但很重要，提供了两个核心函数：

1. **`interpolateTemplate()`** - 模板插值
   ```typescript
   // 示例
   interpolateTemplate("Hello {{name}}", { name: "World" })
   // 结果："Hello World"
   
   interpolateTemplate("{{user.name}}", { user: { name: "Alice" } })
   // 结果："Alice"
   ```

2. **`setByPath()`** - 按路径设置值
   ```typescript
   // 示例
   setByPath({}, "name", "Bob")
   // 结果：{ name: "Bob" }
   
   setByPath({}, "user.name", "Charlie")
   // 结果：{ user: { name: "Charlie" } }
   ```

**实操练习：**
创建一个测试文件 `test-interpolate.ts`：
```typescript
import { interpolateTemplate, setByPath } from './src/lib/utils/interpolate';

// 测试插值
const context = { 
  text: "Hello", 
  user: { name: "Alice", age: 25 } 
};

console.log(interpolateTemplate("{{text}} {{user.name}}", context));
// 输出："Hello Alice"

// 测试路径设置
const result = setByPath(context, "analysis", "This is a greeting");
console.log(result);
// 输出：{ text: "Hello", user: {...}, analysis: "This is a greeting" }
```

#### 2.2 数据库客户端（30分钟）

**阅读文件：`src/lib/prisma.ts` 和 `src/lib/openai.ts`**

理解单例模式的应用：
- **为什么用单例？** 避免创建多个数据库连接
- **全局变量的作用？** 开发模式下复用连接（热重载优化）

#### 2.3 工作流执行引擎（核心！2-3小时）

**阅读文件：`src/lib/engine.ts`**

这是整个系统最复杂也最重要的文件！

**学习步骤：**

**步骤1：理解节点操作器模式（30分钟）**
```typescript
// 操作器接口
interface NodeOperatorArgs {
  node: { id, type, label, config };
  context: ExecutionContext;  // 当前上下文
}

interface NodeOperatorResult {
  output?: unknown;            // 节点输出
  contextPatch?: ExecutionContext;  // 更新的上下文
}
```

**步骤2：学习内置节点类型（1小时）**

1. **输入节点 (input)**
   - 作用：工作流起点，初始化上下文
   - 配置：无
   
2. **AI对话节点 (openai.chat)**
   - 作用：调用AI模型生成文本
   - 配置：
     ```json
     {
       "model": "gpt-4",
       "system": "你是助手",
       "promptTemplate": "请分析：{{text}}",
       "outputKey": "analysis"
     }
     ```
   
3. **设置变量节点 (map.set)**
   - 作用：保存数据到上下文
   - 配置：
     ```json
     {
       "key": "result",
       "valueTemplate": "处理完成：{{analysis}}"
     }
     ```
   
4. **HTTP请求节点 (http.request)**
   - 作用：调用外部API
   - 配置：
     ```json
     {
       "method": "POST",
       "urlTemplate": "https://api.example.com?q={{query}}",
       "bodyTemplate": "{\"data\": \"{{text}}\"}"
     }
     ```

**步骤3：理解拓扑排序算法（1小时）**

**算法目的：** 确定节点的正确执行顺序

**Kahn算法步骤：**
1. 计算每个节点的入度（有多少边指向它）
2. 将入度为0的节点加入队列
3. 逐个取出节点，将其邻居节点入度-1
4. 如果邻居入度变为0，加入队列
5. 重复直到队列为空

**图示示例：**
```
节点：A, B, C, D
连接：A→B, A→C, B→D, C→D

执行顺序：A → B → C → D
（或 A → C → B → D，两种都正确）
```

**步骤4：理解执行流程（30分钟）**

完整流程：
```
1. 加载工作流（nodes + edges）
    ↓
2. 创建Run记录（status: RUNNING）
    ↓
3. 拓扑排序（确定执行顺序）
    ↓
4. 遍历节点列表
    For each 节点:
    ├─ 创建Step记录
    ├─ 获取节点操作器
    ├─ 执行操作器（传入context）
    ├─ 更新context（合并contextPatch）
    ├─ 更新Step状态（SUCCEEDED/FAILED）
    └─ 保存context到Run
    ↓
5. 更新Run状态（SUCCEEDED/FAILED）
    ↓
6. 返回runId
```

**实操练习：**

使用Postman或curl测试完整流程：

```bash
# 1. 创建工作流
curl -X POST http://localhost:3000/api/workflows \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试工作流",
    "description": "学习用",
    "nodes": [
      {
        "type": "input",
        "label": "输入",
        "position": {"x": 100, "y": 100},
        "config": {}
      }
    ],
    "edges": []
  }'

# 2. 运行工作流（使用返回的workflow id）
curl -X POST http://localhost:3000/api/workflows/{workflow_id}/run \
  -H "Content-Type: application/json" \
  -d '{
    "input": {
      "text": "Hello World"
    }
  }'

# 3. 查看运行结果（使用返回的run id）
curl http://localhost:3000/api/runs/{run_id}
```

---

## 🎨 第3阶段：学习前端界面（2-3天）

### 目标
- 理解Next.js的App Router
- 掌握React组件设计
- 学习ReactFlow可视化编辑器

### 学习路径

#### 3.1 API路由（1天）

**API路由架构：**
```
/api
├── /users
│   └── route.ts          # GET /api/users, POST /api/users
├── /workflows
│   ├── route.ts          # GET /api/workflows, POST /api/workflows
│   └── /[id]
│       ├── route.ts      # GET/PUT/DELETE /api/workflows/:id
│       └── /run
│           └── route.ts  # POST /api/workflows/:id/run
└── /runs
    ├── route.ts          # GET /api/runs
    └── /[id]
        └── route.ts      # GET /api/runs/:id
```

**学习顺序：**

1. **`src/app/api/workflows/route.ts`** （30分钟）
   - GET：获取工作流列表
   - POST：创建新工作流
   
2. **`src/app/api/workflows/[id]/route.ts`** （30分钟）
   - GET：获取单个工作流
   - PUT：更新工作流
   - DELETE：删除工作流
   
3. **`src/app/api/workflows/[id]/run/route.ts`** （重要！1小时）
   - POST：运行工作流
   - 调用 `executeWorkflow()` 函数
   
4. **`src/app/api/runs/[id]/route.ts`** （30分钟）
   - GET：获取运行结果

#### 3.2 页面组件（1-2天）

**页面架构：**
```
/                         # 首页（默认Next.js页面）
/workflows                # 工作流列表
/workflows/:id            # 工作流详情
/workflows/:id/editor     # 可视化编辑器 ⭐⭐
/runs/:id                 # 运行结果页
```

**学习顺序：**

**Day 1：列表和详情页**

1. **`src/app/workflows/page.tsx`** （1小时）
   - 功能：展示工作流列表、创建新工作流
   - 技术点：
     - React Hooks（useState, useEffect）
     - 表单处理
     - API调用
   
2. **`src/app/workflows/[id]/page.tsx`** （1小时）
   - 功能：显示工作流详情、运行工作流
   - 技术点：
     - 动态路由参数（useParams）
     - 条件渲染
     - 状态管理
   
3. **`src/app/runs/[id]/page.tsx`** （1小时）
   - 功能：显示运行结果、步骤详情
   - 技术点：
     - 轮询刷新（setInterval）
     - JSON 数据展示
     - 状态样式映射

**Day 2：可视化编辑器（最复杂）**

4. **`src/app/workflows/[id]/editor/page.tsx`** （3-4小时）
   - 功能：拖拽编辑工作流
   - 技术点：
     - ReactFlow 组件
     - 自定义节点
     - 节点连接处理
     - 属性面板

**ReactFlow 核心概念：**
```typescript
import { ReactFlow, Node, Edge } from '@xyflow/react';

// 节点数据结构
const nodes: Node[] = [
  {
    id: 'node-1',
    type: 'custom',  // 自定义节点类型
    position: { x: 100, y: 100 },
    data: {
      type: 'input',
      label: '输入节点',
      config: {}
    }
  }
];

// 边数据结构
const edges: Edge[] = [
  {
    id: 'edge-1',
    source: 'node-1',
    target: 'node-2',
    type: 'smoothstep'
  }
];
```

**实操练习：**
1. 在编辑器中创建一个简单的工作流
2. 添加不同类型的节点
3. 连接节点
4. 配置节点属性
5. 保存并运行

#### 3.3 样式系统（1小时）

**`src/app/globals.css`**
- Tailwind CSS 配置
- 自定义CSS变量
- 深色模式支持

---

## ⚙️ 第4阶段：深入执行引擎（3-4天）

### 目标
- 完全理解工作流执行机制
- 能够添加自定义节点类型
- 理解错误处理和状态管理

### 学习路径

#### 4.1 跟踪完整执行流程（1天）

**使用浏览器开发者工具调试：**

1. 在关键位置添加 `console.log`
2. 创建测试工作流
3. 运行并观察日志

**建议添加日志的位置：**
```typescript
// src/lib/engine.ts

export async function executeWorkflow(...) {
  console.log('🚀 开始执行工作流', params.workflowId);
  
  // ... 加载工作流
  console.log('📦 加载工作流数据', workflow);
  
  // ... 拓扑排序
  console.log('📊 执行顺序', order);
  
  // ... 执行节点
  for (const nodeId of order) {
    console.log('▶️ 执行节点', nodeId);
    // ...
    console.log('✅ 节点执行完成', { output, context });
  }
}
```

#### 4.2 添加自定义节点类型（1-2天）

**实战项目：添加"延时节点"**

**步骤1：注册节点操作器**
```typescript
// src/lib/engine.ts

operatorRegistry.register("delay", async ({ node, context }) => {
  const { seconds = 1 } = node.config || {};
  
  console.log(`⏰ 延时 ${seconds} 秒`);
  await new Promise(resolve => setTimeout(resolve, seconds * 1000));
  
  return { 
    output: `延时${seconds}秒完成`,
    contextPatch: context 
  };
});
```

**步骤2：在编辑器中添加节点类型**
```typescript
// src/app/workflows/[id]/editor/page.tsx

const nodeTemplates = [
  // ... 现有节点
  { 
    type: 'delay', 
    label: '延时节点', 
    description: '延时指定秒数' 
  },
];
```

**步骤3：添加配置UI**
```typescript
{selectedNode.data.type === 'delay' && (
  <div>
    <label>延时秒数</label>
    <input
      type="number"
      value={selectedNode.data.config.seconds || 1}
      onChange={(e) => updateNodeConfig('seconds', Number(e.target.value))}
    />
  </div>
)}
```

**更多节点创意：**
- **条件分支节点**：根据条件选择不同路径
- **循环节点**：重复执行某些步骤
- **数据转换节点**：JSON 解析、格式化等
- **文件操作节点**：读取/写入文件
- **数据库查询节点**：执行SQL查询

#### 4.3 错误处理和调试（1天）

**常见错误类型：**

1. **节点配置错误**
   - API密钥未设置
   - 模板语法错误
   
2. **执行错误**
   - 网络请求失败
   - AI API 超时
   
3. **数据错误**
   - 上下文中缺少必需字段
   - 类型不匹配

**调试技巧：**
```typescript
// 1. 检查上下文
console.log('当前上下文', JSON.stringify(context, null, 2));

// 2. 验证配置
console.log('节点配置', node.config);

// 3. 捕获错误
try {
  // 执行操作
} catch (error) {
  console.error('节点执行失败', {
    nodeId: node.id,
    nodeType: node.type,
    error: error.message,
    context
  });
  throw error;
}
```

---

## 🚀 第5阶段：实践与扩展（持续）

### 实战项目建议

#### 项目1：文档处理工作流（初级）
**目标：** 创建一个文档分析工作流

**节点设计：**
1. 输入节点：接收文档文本
2. AI分析节点：分析文档主题
3. AI总结节点：生成摘要
4. 保存结果节点：保存到上下文

#### 项目2：内容审核工作流（中级）
**目标：** 自动审核用户内容

**节点设计：**
1. 输入节点：接收用户内容
2. 敏感词检测节点：检查违规词汇
3. AI情感分析节点：分析情感倾向
4. HTTP通知节点：发送审核结果
5. 条件分支节点：根据结果采取不同操作

#### 项目3：数据处理管道（高级）
**目标：** 构建复杂的数据ETL流程

**节点设计：**
1. 数据源节点：从API获取数据
2. 数据清洗节点：去重、格式化
3. 数据转换节点：字段映射
4. AI增强节点：使用AI补充信息
5. 数据写入节点：保存到数据库

### 进阶学习方向

#### 方向1：性能优化
- 并行执行节点（无依赖关系的节点同时执行）
- 缓存机制（相同输入复用结果）
- 流式输出（实时显示AI生成内容）

#### 方向2：功能增强
- 用户认证系统
- 工作流模板市场
- 版本控制（工作流历史记录）
- 定时触发（Cron表达式）
- Webhook触发

#### 方向3：AI能力扩展
- 支持更多AI模型（Claude, Gemini等）
- 图像处理节点（识别、生成）
- 语音处理节点（转文字、合成）
- 向量数据库集成（RAG应用）

---

## 📖 推荐学习资源

### 官方文档
1. **Next.js**: https://nextjs.org/docs
2. **Prisma**: https://www.prisma.io/docs
3. **ReactFlow**: https://reactflow.dev/
4. **OpenAI API**: https://platform.openai.com/docs

### 视频教程
1. Next.js 完整教程（B站/YouTube）
2. React Flow 入门教程
3. TypeScript 进阶教程

### 相关开源项目
1. **n8n**: 开源工作流自动化工具
2. **Langflow**: AI应用流程构建器
3. **Flowise**: 拖拽式LLM应用构建

---

## 💡 学习建议

### 学习方法
1. **边学边做**：不要只看代码，要动手修改和实验
2. **提问思考**：为什么这样设计？有没有更好的方案？
3. **写技术笔记**：记录关键概念和疑难点
4. **画图理解**：用流程图、架构图帮助理解

### 每日学习计划
- **早上（1小时）**：阅读源码，理解原理
- **中午（30分钟）**：复习笔记，巩固知识
- **晚上（2小时）**：动手实践，编写代码

### 检验学习成果
每完成一个阶段，尝试回答这些问题：
1. 能否用自己的话解释核心概念？
2. 能否不看代码画出系统架构图？
3. 能否独立完成一个新功能？
4. 能否给别人讲清楚这个系统？

---

## 🎯 总结

### 核心要点
1. **数据模型是基础**：理解数据库模型后，其他都会变得简单
2. **执行引擎是核心**：engine.ts 是最重要的文件
3. **API是桥梁**：连接前端和后端逻辑
4. **编辑器是亮点**：ReactFlow让工作流可视化

### 学习时间估算
- **快速浏览**：1-2天（了解基本功能）
- **深入理解**：7-10天（掌握核心原理）
- **熟练应用**：2-3周（能够扩展功能）
- **精通掌握**：1-2个月（可以重构优化）

### 最后的话
这个项目虽然代码量不大，但涵盖了全栈开发的核心技能：
- 数据库设计
- 后端API开发
- 前端UI构建
- 算法应用（拓扑排序）
- AI集成

认真学习这个项目，你会收获的不仅是一个工具，更是一套完整的解决问题的思路。

**记住：** 编程的本质是解决问题，而不是背诵代码。理解"为什么"比记住"是什么"更重要！

祝学习愉快！💪

---

**问题反馈：** 如果在学习过程中遇到问题，可以：
1. 查看 `PROJECT_GUIDE.md` 获取详细说明
2. 阅读代码注释（每行都有详细说明）
3. 使用浏览器开发者工具调试
4. 在社区提问（Stack Overflow, GitHub Discussions）

