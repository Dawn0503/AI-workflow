# 📂 AI 工作流系统 - 前后端文件分类清单

## 🎯 快速分类指南

在 Next.js 全栈项目中，文件分类规则：

- **🔵 后端文件**：运行在服务器端，处理业务逻辑、数据库操作
- **🟢 前端文件**：运行在浏览器端，处理用户界面、交互
- **⚙️ 共享文件**：前后端都可以使用

---

## 🔵 后端文件（Backend Files）

### 1. 数据库层（Database Layer）

```
prisma/
├── schema.prisma          ✅ 数据库模型定义（最核心！）
├── migrations/            ✅ 数据库迁移文件
└── dev.db                 ✅ SQLite 数据库文件
```

**说明：**
- `schema.prisma`：定义所有数据表结构和关系
- 这些文件只在服务器端使用，浏览器无法访问

---

### 2. 后端核心库（Backend Core Library）

```
src/lib/
├── prisma.ts              ✅ 数据库客户端（后端专用）
├── openai.ts              ✅ AI 客户端（后端专用）
├── engine.ts               ✅ 工作流执行引擎（后端专用，最核心！）
└── utils/
    └── interpolate.ts     ✅ 工具函数（后端使用）
```

**文件说明：**

| 文件 | 作用 | 运行位置 | 为什么是后端 |
|------|------|---------|-------------|
| `prisma.ts` | 创建数据库连接 | 服务器 | 直接访问数据库 |
| `openai.ts` | 调用 AI API | 服务器 | 需要 API Key（不能暴露给前端） |
| `engine.ts` | 执行工作流逻辑 | 服务器 | 处理复杂业务逻辑，访问数据库 |
| `interpolate.ts` | 模板插值工具 | 服务器/客户端 | 主要用于后端，但前端也可能用 |

**关键特征：**
- 这些文件包含数据库操作（`prisma.xxx`）
- 这些文件包含敏感信息（API Key）
- 这些文件处理服务器端业务逻辑

---

### 3. API 路由（API Routes）

```
src/app/api/
├── users/
│   └── route.ts           ✅ 用户 API（GET/POST /api/users）
├── workflows/
│   ├── route.ts           ✅ 工作流列表 API（GET/POST /api/workflows）
│   └── [id]/
│       ├── route.ts       ✅ 单个工作流 API（GET/PUT/DELETE /api/workflows/:id）
│       └── run/
│           └── route.ts  ✅ 运行工作流 API（POST /api/workflows/:id/run）
└── runs/
    ├── route.ts           ✅ 运行记录列表 API（GET /api/runs）
    └── [id]/
        └── route.ts       ✅ 单个运行记录 API（GET /api/runs/:id）
```

**识别方法：**
- 路径包含 `/api/` 的都是后端 API
- 文件名为 `route.ts`（Next.js App Router 约定）
- 导出 `GET`, `POST`, `PUT`, `DELETE` 等函数

**示例代码：**
```typescript
// src/app/api/workflows/route.ts
// 这是后端文件，运行在服务器上

export async function GET() {
  // 服务器端代码：直接访问数据库
  const workflows = await prisma.workflow.findMany();
  return NextResponse.json(workflows);
}
```

---

## 🟢 前端文件（Frontend Files）

### 1. 页面组件（Page Components）

```
src/app/
├── page.tsx               ✅ 首页（/）
├── layout.tsx             ✅ 根布局（所有页面共用）
├── globals.css            ✅ 全局样式
│
├── workflows/
│   ├── page.tsx           ✅ 工作流列表页（/workflows）
│   └── [id]/
│       ├── page.tsx       ✅ 工作流详情页（/workflows/:id）
│       └── editor/
│           └── page.tsx   ✅ 可视化编辑器（/workflows/:id/editor）
│
└── runs/
    └── [id]/
        └── page.tsx       ✅ 运行结果页（/runs/:id）
```

**识别方法：**
- 路径不包含 `/api/`
- 文件名为 `page.tsx`（Next.js 页面组件）
- 通常包含 `"use client"` 指令（客户端组件）
- 使用 React Hooks（`useState`, `useEffect` 等）

**示例代码：**
```typescript
// src/app/workflows/page.tsx
// 这是前端文件，运行在浏览器中

"use client";  // 标记为客户端组件

export default function WorkflowsPage() {
  const [workflows, setWorkflows] = useState([]);
  
  // 浏览器端代码：通过 HTTP 请求获取数据
  useEffect(() => {
    fetch('/api/workflows')
      .then(r => r.json())
      .then(setWorkflows);
  }, []);
  
  return <div>...</div>;
}
```

---

## ⚙️ 共享文件（Shared Files）

### 配置文件（Configuration Files）

```
根目录/
├── package.json           ⚙️ 项目依赖配置（前后端共用）
├── tsconfig.json          ⚙️ TypeScript 配置（前后端共用）
├── next.config.ts         ⚙️ Next.js 配置（前后端共用）
├── .env                   ⚙️ 环境变量（服务器端使用）
└── ...
```

**说明：**
- 这些文件配置整个项目
- 前后端都遵循这些配置
- `.env` 文件只在服务器端读取（不会发送到浏览器）

---

## 📊 完整文件清单

### 🔵 后端文件列表（共 11 个文件）

#### 数据库层（1 个）
- [x] `prisma/schema.prisma`

#### 核心库（4 个）
- [x] `src/lib/prisma.ts`
- [x] `src/lib/openai.ts`
- [x] `src/lib/engine.ts`
- [x] `src/lib/utils/interpolate.ts`

#### API 路由（6 个）
- [x] `src/app/api/users/route.ts`
- [x] `src/app/api/workflows/route.ts`
- [x] `src/app/api/workflows/[id]/route.ts`
- [x] `src/app/api/workflows/[id]/run/route.ts`
- [x] `src/app/api/runs/route.ts`
- [x] `src/app/api/runs/[id]/route.ts`

---

### 🟢 前端文件列表（共 6 个文件）

#### 布局和样式（2 个）
- [x] `src/app/layout.tsx`
- [x] `src/app/globals.css`

#### 页面组件（4 个）
- [ ] `src/app/page.tsx`（首页，简单）
- [x] `src/app/workflows/page.tsx`（工作流列表）
- [ ] `src/app/workflows/[id]/page.tsx`（工作流详情）
- [ ] `src/app/workflows/[id]/editor/page.tsx`（可视化编辑器）
- [ ] `src/app/runs/[id]/page.tsx`（运行结果）

---

## 🔍 如何快速判断文件是前端还是后端？

### 判断方法 1：看文件路径

```
包含 /api/  → 🔵 后端
不包含 /api/ → 🟢 前端（页面组件）
```

### 判断方法 2：看文件内容

**后端文件特征：**
```typescript
// ✅ 后端文件特征
import { prisma } from "@/lib/prisma";  // 使用数据库
import { NextResponse } from "next/server";  // Next.js 服务器 API

export async function GET() {  // 导出 HTTP 方法
  const data = await prisma.xxx.findMany();  // 数据库操作
  return NextResponse.json(data);  // 返回 JSON
}
```

**前端文件特征：**
```typescript
// ✅ 前端文件特征
"use client";  // 客户端组件标记

import { useState, useEffect } from "react";  // React Hooks

export default function Page() {
  const [data, setData] = useState([]);  // 状态管理
  
  useEffect(() => {
    fetch('/api/xxx').then(...);  // HTTP 请求
  }, []);
  
  return <div>...</div>;  // JSX 渲染
}
```

### 判断方法 3：看文件命名

```
route.ts  → 🔵 后端（API 路由）
page.tsx  → 🟢 前端（页面组件）
layout.tsx → 🟢 前端（布局组件）
```

---

## 📋 前后端交互示意图

```
┌─────────────────────────────────────────────────┐
│           🟢 前端文件（浏览器）                    │
│  src/app/workflows/page.tsx                     │
│  - 显示 UI                                      │
│  - 用户交互                                     │
│  - 发送 HTTP 请求                               │
└─────────────────┬───────────────────────────────┘
                  │ HTTP 请求
                  │ fetch('/api/workflows')
                  ↓
┌─────────────────────────────────────────────────┐
│           🔵 后端文件（服务器）                    │
│  src/app/api/workflows/route.ts                 │
│  - 接收 HTTP 请求                               │
│  - 调用业务逻辑                                 │
└─────────────────┬───────────────────────────────┘
                  │ 调用
                  ↓
┌─────────────────────────────────────────────────┐
│           🔵 后端核心库（服务器）                  │
│  src/lib/engine.ts                              │
│  src/lib/prisma.ts                              │
│  - 业务逻辑处理                                 │
│  - 数据库操作                                   │
└─────────────────┬───────────────────────────────┘
                  │ 查询/写入
                  ↓
┌─────────────────────────────────────────────────┐
│           🔵 数据库（服务器）                     │
│  prisma/schema.prisma                           │
│  prisma/dev.db                                  │
│  - 数据存储                                     │
└─────────────────────────────────────────────────┘
```

---

## 💡 学习建议

### 如果你想学习后端：

**必读文件（按顺序）：**
1. `prisma/schema.prisma` - 理解数据结构
2. `src/lib/prisma.ts` - 理解数据库连接
3. `src/lib/engine.ts` - 理解核心业务逻辑（最重要！）
4. `src/app/api/workflows/[id]/run/route.ts` - 理解 API 如何调用业务逻辑

### 如果你想学习前端：

**必读文件（按顺序）：**
1. `src/app/layout.tsx` - 理解页面结构
2. `src/app/workflows/page.tsx` - 理解如何调用 API
3. `src/app/workflows/[id]/editor/page.tsx` - 理解复杂交互

### 如果你想理解全栈：

**完整流程示例：**
1. 前端：`src/app/workflows/page.tsx` - 用户点击"创建"
2. 后端：`src/app/api/workflows/route.ts` - 接收请求
3. 后端：`src/lib/prisma.ts` - 操作数据库
4. 后端：返回 JSON 数据
5. 前端：更新 UI 显示

---

## 🎯 总结

### 后端文件（11 个）
- ✅ 数据库：`prisma/schema.prisma`
- ✅ 核心库：`src/lib/` 下的所有文件
- ✅ API 路由：`src/app/api/` 下的所有文件

### 前端文件（6 个）
- ✅ 布局：`src/app/layout.tsx`, `src/app/globals.css`
- ✅ 页面：`src/app/` 下除了 `api/` 外的所有 `page.tsx` 文件

### 关键区别
- **后端**：处理数据、业务逻辑、数据库操作
- **前端**：处理界面、用户交互、HTTP 请求

---

**文档最后更新：** 2025年1月  
**文件总数：** 后端 11 个 + 前端 6 个 = 17 个核心文件

