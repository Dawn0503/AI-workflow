# 🗂️ AI 工作流系统 - 前后端文件分类说明

## 📊 项目架构总览

```
AI-workflow/
├── 🔵 后端文件
│   ├── prisma/                    # 数据库相关
│   ├── src/lib/                   # 后端核心库
│   └── src/app/api/               # API 路由（后端接口）
│
├── 🟢 前端文件
│   ├── src/app/page.tsx           # 首页
│   ├── src/app/layout.tsx         # 根布局
│   ├── src/app/globals.css        # 全局样式
│   ├── src/app/workflows/         # 工作流相关页面
│   └── src/app/runs/              # 运行结果页面
│
└── ⚙️ 配置文件
    ├── package.json
    ├── tsconfig.json
    └── next.config.ts
```

---

## 🔵 后端文件详解

### 1. 数据库层（Database Layer）

#### `prisma/schema.prisma` ✅ 已注释
**类型：** 数据库模型定义  
**作用：** 定义所有数据表结构和关系  
**关键内容：**
- 6个模型：User, Workflow, WorkflowNode, WorkflowEdge, Run, Step
- 关系定义和级联删除
- 索引优化

---

### 2. 后端核心库（Backend Core Library）

#### `src/lib/prisma.ts` ✅ 已注释
**类型：** 数据库客户端  
**作用：** 创建 Prisma 数据库客户端单例  
**关键技术：** 单例模式、全局变量

#### `src/lib/openai.ts` ✅ 已注释
**类型：** AI 客户端  
**作用：** 创建 OpenAI 客户端单例  
**关键技术：** 单例模式、环境变量配置

#### `src/lib/utils/interpolate.ts` ✅ 已注释
**类型：** 工具函数  
**作用：** 提供模板插值和路径设置功能  
**关键函数：**
- `interpolateTemplate()` - 模板插值
- `setByPath()` - 按路径设置值

#### `src/lib/engine.ts` ✅ 已注释 ⭐⭐⭐
**类型：** 工作流执行引擎（最核心！）  
**作用：** 实现工作流的完整执行逻辑  
**关键内容：**
- 节点操作器注册表
- 4种内置节点实现
- 拓扑排序算法
- 执行流程控制

---

### 3. API 路由层（API Routes Layer）

#### `src/app/api/users/route.ts` ✅ 已注释
**路由：** `GET/POST /api/users`  
**作用：** 用户管理（预留功能）

#### `src/app/api/workflows/route.ts` ✅ 已注释
**路由：** `GET/POST /api/workflows`  
**作用：** 
- GET: 获取工作流列表
- POST: 创建新工作流

#### `src/app/api/workflows/[id]/route.ts` ✅ 已注释
**路由：** `GET/PUT/DELETE /api/workflows/:id`  
**作用：**
- GET: 获取单个工作流详情
- PUT: 更新工作流
- DELETE: 删除工作流

#### `src/app/api/workflows/[id]/run/route.ts` ✅ 已注释
**路由：** `POST /api/workflows/:id/run`  
**作用：** 触发工作流执行（最重要的API）

#### `src/app/api/runs/route.ts` ✅ 已注释
**路由：** `GET /api/runs`  
**作用：** 获取运行记录列表

#### `src/app/api/runs/[id]/route.ts` ✅ 已注释
**路由：** `GET /api/runs/:id`  
**作用：** 获取单个运行记录详情（含步骤）

---

## 🟢 前端文件详解

### 1. 布局和样式

#### `src/app/layout.tsx` ✅ 已注释
**类型：** React 根布局组件  
**作用：** 定义整个应用的 HTML 结构  
**关键内容：** 字体配置、元数据

#### `src/app/globals.css` ✅ 已注释
**类型：** 全局样式表  
**作用：** 定义 CSS 变量、主题、深色模式

---

### 2. 页面组件（需要添加注释！）

#### `src/app/page.tsx` ⚠️ 待注释
**路由：** `/`（首页）  
**类型：** React 页面组件  
**作用：** 应用首页（Next.js 默认欢迎页）  
**状态：** 简单页面，可保持默认

#### `src/app/workflows/page.tsx` ⚠️ 待注释
**路由：** `/workflows`  
**类型：** React 页面组件  
**作用：** 工作流列表页  
**功能：**
- 显示所有工作流
- 创建新工作流表单
- 跳转到编辑器/详情页

#### `src/app/workflows/[id]/page.tsx` ⚠️ 待注释
**路由：** `/workflows/:id`  
**类型：** React 页面组件  
**作用：** 工作流详情页  
**功能：**
- 显示工作流信息
- 输入数据运行工作流
- 查看节点组成

#### `src/app/workflows/[id]/editor/page.tsx` ⚠️ 待注释
**路由：** `/workflows/:id/editor`  
**类型：** React 页面组件（最复杂！）  
**作用：** 可视化工作流编辑器  
**功能：**
- ReactFlow 拖拽编辑
- 添加/删除/配置节点
- 连接节点
- 保存工作流

#### `src/app/runs/[id]/page.tsx` ⚠️ 待注释
**路由：** `/runs/:id`  
**类型：** React 页面组件  
**作用：** 运行结果详情页  
**功能：**
- 显示运行状态
- 展示每个步骤详情
- 实时轮询更新

---

## ⚙️ 配置文件

#### `package.json`
**类型：** NPM 配置文件  
**作用：** 项目依赖和脚本定义

#### `tsconfig.json`
**类型：** TypeScript 配置  
**作用：** TypeScript 编译选项

#### `next.config.ts`
**类型：** Next.js 配置  
**作用：** Next.js 框架配置

#### `eslint.config.mjs`
**类型：** ESLint 配置  
**作用：** 代码规范检查

#### `postcss.config.mjs`
**类型：** PostCSS 配置  
**作用：** CSS 处理配置

---

## 📋 注释完成情况

### ✅ 已完成注释（后端）
- [x] `prisma/schema.prisma`
- [x] `src/lib/prisma.ts`
- [x] `src/lib/openai.ts`
- [x] `src/lib/utils/interpolate.ts`
- [x] `src/lib/engine.ts`
- [x] `src/app/api/users/route.ts`
- [x] `src/app/api/workflows/route.ts`
- [x] `src/app/api/workflows/[id]/route.ts`
- [x] `src/app/api/workflows/[id]/run/route.ts`
- [x] `src/app/api/runs/route.ts`
- [x] `src/app/api/runs/[id]/route.ts`

### ✅ 已完成注释（前端基础）
- [x] `src/app/layout.tsx`
- [x] `src/app/globals.css`

### ⚠️ 待完成注释（前端页面）
- [ ] `src/app/page.tsx`
- [ ] `src/app/workflows/page.tsx`
- [ ] `src/app/workflows/[id]/page.tsx`
- [ ] `src/app/workflows/[id]/editor/page.tsx`
- [ ] `src/app/runs/[id]/page.tsx`

---

## 🎯 前后端交互流程

### 完整的请求流程

```
🟢 前端页面
    ↓ HTTP 请求
🔵 API 路由（后端接口）
    ↓ 调用
🔵 核心库（业务逻辑）
    ↓ 查询/写入
🔵 数据库（Prisma + SQLite）
    ↓ 返回数据
🔵 核心库
    ↓ 返回 JSON
🔵 API 路由
    ↓ HTTP 响应
🟢 前端页面（显示结果）
```

### 具体示例：运行工作流

```
1. 🟢 用户在详情页输入数据，点击"运行"
   /workflows/[id]/page.tsx

2. 🟢 前端发送 POST 请求
   fetch('/api/workflows/123/run', { body: { input: {...} } })

3. 🔵 API 路由接收请求
   src/app/api/workflows/[id]/run/route.ts

4. 🔵 调用执行引擎
   executeWorkflow({ workflowId, input })
   src/lib/engine.ts

5. 🔵 执行引擎操作数据库
   - 创建 Run 记录
   - 创建 Step 记录
   - 执行节点逻辑
   prisma.run.create()

6. 🔵 返回运行记录 ID
   { runId: "run_123" }

7. 🟢 前端跳转到结果页
   /runs/run_123

8. 🟢 结果页轮询查询状态
   fetch('/api/runs/run_123') 每2秒

9. 🔵 API 返回最新状态
   GET /api/runs/[id]/route.ts

10. 🟢 前端更新显示
    显示执行进度和结果
```

---

## 💡 学习建议

### 学习顺序建议

**第1步：理解后端逻辑**（重要！）
```
后端是基础，前端只是展示层
先理解后端如何工作，再看前端如何调用
```

**第2步：学习前后端交互**
```
看一个完整的流程：
前端发请求 → API接收 → 调用业务逻辑 → 返回结果 → 前端展示
```

**第3步：学习前端页面**
```
理解React组件如何使用API
理解状态管理和数据流
```

---

## 🔍 快速定位

### 想了解工作流如何执行？
→ 看后端：`src/lib/engine.ts` ⭐⭐⭐

### 想了解如何触发执行？
→ 看后端API：`src/app/api/workflows/[id]/run/route.ts`
→ 看前端页面：`src/app/workflows/[id]/page.tsx`

### 想了解如何编辑工作流？
→ 看前端：`src/app/workflows/[id]/editor/page.tsx`
→ 看后端API：`src/app/api/workflows/[id]/route.ts`

### 想了解数据如何存储？
→ 看数据库：`prisma/schema.prisma`
→ 看数据库客户端：`src/lib/prisma.ts`

---

## 📊 技术栈分类

### 🔵 后端技术
- **框架**：Next.js API Routes
- **数据库 ORM**：Prisma
- **数据库**：SQLite
- **AI 集成**：OpenAI SDK
- **语言**：TypeScript

### 🟢 前端技术
- **框架**：React 19 + Next.js 15
- **UI 库**：Tailwind CSS
- **可视化**：ReactFlow
- **状态管理**：React Hooks
- **语言**：TypeScript

---

## 🎯 下一步

我将为所有前端页面文件添加详细注释！

预计添加注释的文件：
1. `src/app/workflows/page.tsx` - 工作流列表页
2. `src/app/workflows/[id]/page.tsx` - 工作流详情页
3. `src/app/workflows/[id]/editor/page.tsx` - 可视化编辑器
4. `src/app/runs/[id]/page.tsx` - 运行结果页

这些是前端最重要的文件，马上为你添加详细注释！







