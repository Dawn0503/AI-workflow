# AI 工作流系统使用指南

## 📋 目录
1. [快速开始](#快速开始)
2. [系统功能](#系统功能)
3. [使用流程](#使用流程)
4. [节点类型说明](#节点类型说明)
5. [API 接口文档](#api-接口文档)
6. [常见问题](#常见问题)

---

## 🚀 快速开始

### 1. 环境准备

确保你已经：
- ✅ 安装了 Node.js（推荐 18+ 版本）
- ✅ 配置了 OpenAI API Key（在 `.env` 文件中）
- ✅ 运行了数据库迁移

### 2. 启动项目

```bash
# 安装依赖（如果还没安装）
npm install

# 启动开发服务器
npm run dev
```

### 3. 访问系统

打开浏览器访问：`http://localhost:3000`

---

## 🎯 系统功能

### 前端页面

1. **工作流列表页** (`/workflows`)
   - 查看所有工作流
   - 创建新工作流
   - 跳转到编辑器和详情页

2. **工作流详情页** (`/workflows/[id]`)
   - 查看工作流信息
   - 运行工作流
   - 查看节点组成

3. **可视化编辑器** (`/workflows/[id]/editor`)
   - 拖拽添加节点
   - 连接节点创建流程
   - 配置节点参数
   - 保存工作流

4. **运行记录页** (`/runs/[id]`)
   - 查看执行结果
   - 查看每个节点的执行详情

---

## 📝 使用流程

### 步骤 1：创建工作流

**方法一：通过前端页面**
1. 访问 `http://localhost:3000/workflows`
2. 填写工作流名称和描述
3. 点击"创建工作流"按钮

**方法二：通过 API**
```bash
curl -X POST http://localhost:3000/api/workflows \
  -H "Content-Type: application/json" \
  -d '{
    "name": "文本摘要工作流",
    "description": "使用 AI 对文本进行摘要",
    "nodes": [
      {
        "type": "input",
        "label": "输入节点"
      }
    ],
    "edges": []
  }'
```

### 步骤 2：编辑工作流

1. 在工作流列表页点击"编辑"按钮
2. 或访问 `http://localhost:3000/workflows/[id]/editor`

**编辑器操作：**
- **添加节点**：点击左侧工具栏中的节点类型（如"AI对话"、"设置变量"等）
- **连接节点**：从节点的底部拖拽到另一个节点的顶部
- **移动节点**：直接拖拽节点到新位置
- **配置节点**：点击节点，在右侧属性面板中编辑配置
- **删除节点**：选中节点后，点击右侧的"删除"按钮
- **保存工作流**：点击左上角的"保存工作流"按钮

### 步骤 3：配置节点

#### AI 对话节点 (openai.chat)

**配置项：**
- **模型名称**：如 `gpt-4o-mini`、`gpt-4`、`gpt-3.5-turbo`
- **系统提示词**：定义 AI 的角色和行为（可选）
- **提示模板**：支持 `{{variable}}` 插值，例如：
  ```
  请总结以下内容：{{text}}
  ```
- **输出键名**：AI 回复保存到上下文的键名，例如 `summary`

**示例配置：**
```json
{
  "model": "gpt-4o-mini",
  "system": "你是一个专业的文本分析师",
  "promptTemplate": "请用一句话总结：{{text}}",
  "outputKey": "summary"
}
```

#### 设置变量节点 (map.set)

**配置项：**
- **键名**：要设置的键名，支持嵌套路径（如 `result.data`）
- **值模板**：支持 `{{variable}}` 插值

**示例配置：**
```json
{
  "key": "final_result",
  "valueTemplate": "处理结果：{{summary}}"
}
```

#### HTTP 请求节点 (http.request)

**配置项：**
- **请求方法**：GET、POST、PUT、PATCH、DELETE
- **URL 模板**：支持 `{{variable}}` 插值
- **请求体模板**：JSON 格式，支持插值（仅非 GET 请求）

**示例配置：**
```json
{
  "method": "POST",
  "urlTemplate": "https://api.example.com/process",
  "bodyTemplate": "{\"text\": \"{{text}}\"}"
}
```

### 步骤 4：运行工作流

**方法一：通过前端页面**
1. 访问工作流详情页：`http://localhost:3000/workflows/[id]`
2. 在"运行工作流"区域输入文本
3. 点击"运行工作流"按钮
4. 等待执行完成后，会自动跳转到结果页面

**方法二：通过 API**
```bash
curl -X POST http://localhost:3000/api/workflows/[id]/run \
  -H "Content-Type: application/json" \
  -d '{
    "input": {
      "text": "这是一段需要处理的文本内容"
    }
  }'
```

**响应示例：**
```json
{
  "runId": "run_abc123"
}
```

### 步骤 5：查看执行结果

访问运行记录页：`http://localhost:3000/runs/[runId]`

可以查看：
- 执行状态（成功/失败）
- 最终上下文数据
- 每个节点的执行详情（输入、输出、错误信息）

---

## 🔧 节点类型说明

### 1. 输入节点 (input)

- **作用**：工作流的起始点，将初始输入数据设置为上下文
- **配置**：无需配置
- **使用场景**：每个工作流都应该有一个输入节点

### 2. AI 对话节点 (openai.chat)

- **作用**：调用 OpenAI API 进行文本生成
- **配置**：模型、系统提示词、提示模板、输出键名
- **使用场景**：文本摘要、翻译、分析、生成等

### 3. 设置变量节点 (map.set)

- **作用**：将值保存到上下文中的指定路径
- **配置**：键名、值模板
- **使用场景**：数据转换、结果格式化

### 4. HTTP 请求节点 (http.request)

- **作用**：发送 HTTP 请求到外部 API
- **配置**：请求方法、URL 模板、请求体模板
- **使用场景**：调用外部服务、数据获取

---

## 📡 API 接口文档

### 获取工作流列表

```http
GET /api/workflows
```

**响应：**
```json
[
  {
    "id": "workflow123",
    "name": "文本摘要工作流",
    "description": "使用 AI 对文本进行摘要",
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-01-01T00:00:00.000Z",
    "nodes": [...],
    "edges": [...]
  }
]
```

### 创建工作流

```http
POST /api/workflows
Content-Type: application/json

{
  "name": "工作流名称",
  "description": "工作流描述",
  "nodes": [
    {
      "type": "input",
      "label": "输入节点",
      "position": { "x": 0, "y": 0 },
      "config": {}
    }
  ],
  "edges": []
}
```

### 获取工作流详情

```http
GET /api/workflows/[id]
```

### 更新工作流

```http
PUT /api/workflows/[id]
Content-Type: application/json

{
  "name": "更新后的名称",
  "description": "更新后的描述",
  "nodes": [...],
  "edges": [...]
}
```

### 删除工作流

```http
DELETE /api/workflows/[id]
```

### 运行工作流

```http
POST /api/workflows/[id]/run
Content-Type: application/json

{
  "input": {
    "text": "要处理的文本"
  }
}
```

**响应：**
```json
{
  "runId": "run_abc123"
}
```

### 获取运行记录

```http
GET /api/runs/[id]
```

**响应：**
```json
{
  "id": "run_abc123",
  "workflowId": "workflow123",
  "status": "SUCCEEDED",
  "startedAt": "2025-01-01T00:00:00.000Z",
  "finishedAt": "2025-01-01T00:01:00.000Z",
  "input": { "text": "..." },
  "context": { "text": "...", "summary": "..." },
  "steps": [...]
}
```

---

## 💡 使用示例

### 示例 1：简单的文本摘要工作流

**工作流结构：**
```
输入节点 → AI对话节点
```

**配置：**
1. **输入节点**：无需配置
2. **AI对话节点**：
   - 模型：`gpt-4o-mini`
   - 提示模板：`请用一句话总结：{{text}}`
   - 输出键名：`summary`

**运行：**
```json
{
  "input": {
    "text": "这是一段很长的文本内容..."
  }
}
```

**结果：**
```json
{
  "text": "这是一段很长的文本内容...",
  "summary": "这是一段关于...的文本"
}
```

### 示例 2：文本分析 + 结果格式化

**工作流结构：**
```
输入节点 → AI对话节点 → 设置变量节点
```

**配置：**
1. **输入节点**：无需配置
2. **AI对话节点**：
   - 模型：`gpt-4o-mini`
   - 系统提示词：`你是一个专业的文本分析师`
   - 提示模板：`请分析以下文本的情感倾向：{{text}}`
   - 输出键名：`analysis`
3. **设置变量节点**：
   - 键名：`final_result`
   - 值模板：`分析结果：{{analysis}}`

---

## ❓ 常见问题

### Q1: 如何设置 OpenAI API Key？

在项目根目录创建 `.env` 文件：
```
OPENAI_API_KEY=sk-your-api-key-here
DATABASE_URL="file:./prisma/dev.db"
```

### Q2: 工作流执行失败怎么办？

1. 检查运行记录页面的错误信息
2. 查看每个节点的执行详情
3. 常见原因：
   - OpenAI API Key 未配置或无效
   - 节点配置错误（如提示模板中的变量不存在）
   - 网络连接问题

### Q3: 如何查看工作流的执行历史？

访问运行记录列表（如果实现了）或通过 API：
```bash
GET /api/runs?workflowId=[id]
```

### Q4: 支持哪些 AI 模型？

支持所有 OpenAI 支持的模型，如：
- `gpt-4o-mini`（推荐，性价比高）
- `gpt-4`
- `gpt-3.5-turbo`
- `gpt-4-turbo`

### Q5: 如何添加自定义节点类型？

需要修改 `src/lib/engine.ts` 文件，在 `operatorRegistry` 中注册新的节点操作器。

---

## 🎓 进阶技巧

### 1. 使用模板插值

在提示模板、值模板、URL 模板中，可以使用 `{{variable}}` 来引用上下文中的数据：

```javascript
// 上下文
{
  "text": "Hello",
  "user": { "name": "Alice" }
}

// 模板
"{{text}} from {{user.name}}"

// 结果
"Hello from Alice"
```

### 2. 嵌套路径

支持使用点号访问嵌套对象：
- `{{user.name}}` - 访问 `context.user.name`
- `{{result.data.summary}}` - 访问 `context.result.data.summary`

### 3. 工作流设计建议

- **从简单开始**：先创建一个简单的输入→AI对话工作流
- **逐步扩展**：添加更多节点和连接
- **测试每个节点**：确保每个节点配置正确
- **保存备份**：重要的工作流可以导出配置（JSON 格式）

---

## 📚 相关文档

- [项目 README](./README.md)
- [代码阅读指南](./代码阅读指南.md)
- [Prisma Schema](./prisma/schema.prisma)

---

**祝你使用愉快！** 🎉

